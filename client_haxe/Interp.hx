package;

class Interp {
    
    // go, move, wait, forward, if, not,
    // spelled-out numbers, numbers,
    public static inline function interp (string : String) {
        
        var space = ' ';
        
        var sentence =
            string.
            split (space);
            
        for (word in sentence)
            
    }
}